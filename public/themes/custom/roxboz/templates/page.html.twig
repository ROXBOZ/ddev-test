{#
/**
 * @file
 * Roxboz theme implementation to display a single page.
 *
 * Available variables:
 * - page: A render array representing the page content.
 */
#}
<div class="page-wrapper">
  {# <header class="site-header">
    <div class="container">
      {% if page.header %}
        {{ page.header }}
      {% endif %}
      
      <div class="site-branding">
        <h1 class="site-title">
          <a href="{{ path('<front>') }}" class="btn btn--outline">
            {{ site_name|default('My Site') }}
          </a>
        </h1>
      </div>
    </div>
  </header> #}

  <main class="main-content">
    <div class="container">
      {# {% if page.highlighted %}
        <div class="highlighted">
          {{ page.highlighted }}
        </div>
      {% endif %} #}

      <div style="background-color:red">
       {% if page.content %}
        {{ page.content }} 
       {% endif %}
      </div>
    </div>
  </main>

  <footer class="footer">
    <div>
      {% if page.footer %}
        {{ page.footer }}
      {% endif %}
      <p class="footer__copyright">&copy; {{ "now"|date("Y") }} ROXBOZ Drupal Testing</p>
    </div>
  </footer>
</div>
